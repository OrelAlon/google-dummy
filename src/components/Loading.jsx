import { Puff } from "react-loader-spinner";

const Loading = () => {
  return (
    <div>
      <Puff color="#00BFFF" height={55} width={80} />
    </div>
  );
};

export default Loading;
